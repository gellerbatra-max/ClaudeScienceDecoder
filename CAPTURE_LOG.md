# Capture log — round 2

One line per capture: item id · what was done in PDS · verifier verdict · anything the GUI did that AGENT_KNOWLEDGE_BASE.md does not already describe.

Session start 2026-09-07. `python selftest.py` → SELFTEST PASS (Python 3.14.7) before PDS was touched.
Two PDS instances were already running at session start: PID 15440 `Work Area 1 - Pattern Design`, PID 4064 `Work Area 2 - 1 piece(s) - Pattern Design`.

| item | PDS actions | verifier | new GUI notes |
|---|---|---|---|
| V3-PROD-MOCUP-B1-1-NOEDIT | AccuMark V17 DCU export of production model `2303 MOCUP B1 1` from `Test` to ASTM DXF, with no piece edit. The native piece members are byte-exact `a1DCF.tmp` (INMO), `a1EB3.tmp` (OUMO), and model member `a2905.tmp` extracted from the earlier native `2303 PATTERN-MODELS.zip`; the outer checkpoint ZIP was reconstructed because DCU's native-ZIP backend stalled after staging `ver.5`. | `RESULT: PASS` with `verify_capture.py --internal-layers`: **20/20** lists match. Both grain lists map to layer 7; ten `0x49` lists map to layer 8; eight `0x48` lists map to layer 11, with seven zero residuals and one 0.0001-in rounding residual. Rechecking the existing production OUCF capture gives 2/2: grain on layer 7 and `0x4d` on layer 6 with zero residual. This supersedes the old provisional names: `0x49=internal`, `0x48=internal_cutout`, `0x4d=mirror`. | DCU's ASTM path completed reliably and reported 1/1 model, 2/2 pieces, 1/1 rule table. Its native ZIP path twice reached a 100% staging display but emitted only a temporary `ver.5`; preserve that distinction in provenance instead of treating the GUI percentage as export evidence. |
| CAP-C00-BASE | Work Area 1, Create→Rectangle, corner (150,500)→(350,633) px at ~6.66 px/cm = 30.05 × 19.99 cm; named at the creation prompt; File→Save As→CAP-C00-BASE; File→Export ASTM then ZIP into the pre-made folder. Piece not moved afterwards. | `RESULT: PASS` — 1542 bytes, piece_records=1, perimeter 4, notches 0, seam 0, dxf residual 0.000000, category=CAP-C00-BASE, annotation RECTANGLE, rule_table A1-LADIES (default), 7 sizes base 12 | (1) File menu is a plain dropdown here, not a full-screen backstage; Save As at (54,262), Export at (50,672). (2) Export dialog accepts a full path typed into File name — no folder navigation needed. (3) PDS appends `.DXF` to whatever you type: `NAME.dxf` → `NAME.dxf.DXF` and the rule file becomes `NAME.RUL.DXF`. Type the bare name. Renamed on disk afterwards. (4) Rectangle hover readout puts width in the **X** column of the Beg row and height (negative, downward) in the **Y** column of the End row — not the Beg/End columns. (5) A fresh piece's category equals its own name — confirms the Copy/Paste contamination theory from the other side. |
| CAP-C01-REEXPORT | Same piece, no edit, no re-save, no move: File→Export ZIP then ASTM into `CAP-C01-REEXPORT/` (files keep the piece name `CAP-C00-BASE.*`, ~2 min after C00's export). | `RESULT: PASS` — 1542→1542 bytes, **1 differing run, 0 structural**: 3 bytes of heap residue at 0x48 (`50f85c`→`102c5d`). Timestamps identical → the trailer u32 is the last-saved time, not the export time. DXF geometry matches (0.000000) but the DXF's absolute coordinates shifted between the two exports while the binary did not. | Export dialog remembers the last "Save as type" (ZIP stayed selected). Filename autocomplete popup can cover the type combo after typing a full path; clicking Save still works. `Include Grade Rule Table` is greyed out on an ungraded piece. |
| CAP-C50-DRILL1 | On the unmoved C00 piece: Create→Point group→Point/Drill (55,74), one click inside the piece at (290,605) px, then **OK** in the panel (a blue `+` stayed on the piece after the cursor moved away). File→Save As→CAP-C50-DRILL1 (piece click, retype name, Save). Export ASTM then ZIP. | `RESULT: PASS` — structural_change=**yes** (17 runs, 9 structural), 1542→2781 bytes. DXF gained `POINT`+`TEXT` on layer 13. **piece_records=2** although the piece was never copy-pasted; category stays `CAP-C00-BASE`. 45 bytes inserted at 0x1b6 (`ffff 4400 0100 0100 0000 ffff 8a43 0100 b640 0000 0100 0000 …`) — a new internal list of type 0x44 with one unnumbered point. | Point/Drill tool: the click places a pending mark; clicking **OK** commits it and exits the tool. Round 1 probably lost the point by leaving via Cancel. Export Results window can lag the screenshot by a second or two — check the window list, not just the image. |
| CAP-C30-SEAM-UNEVEN | **Applied to the wrong piece**: after re-opening CAP-C00-BASE the view auto-zoomed to fit all pieces and I seamed the round-1 `TASK1-CUTQTY3` rectangle at the top-left instead (status bar said so; I dismissed it as hover noise). Advanced→Seam→Define, piece click, radio Manual-Uneven, then per corner: click corner → type value → **OK** (Apply is disabled in this mode; OK commits the point and loops back to "Select point(s)"): TL=1.0, TR=0.5, BR=0.25, BL untouched. Final OK with nothing selected ends the tool and keeps the seam. Save As, export ASTM + ZIP. | `RESULT: PASS` — uneven_seam=yes, dxf residual 0 vs layer 14, 3444 bytes, piece_records=2, category TASK1-CUTQTY3. Segments decode as (begin,end) = (0,3937), (0,1969), (0,984), and **no seam flag** on the fourth. Cut-line records: 3 (the 0→0 edge has none); the left cut line runs (3938,1)→(4,78749), i.e. a linear taper from 0 at BL to 1 cm at TL. So a corner value sets the allowance at the **end** of the segment that ends at that corner; `begin` is the allowance at the segment's first point. C31's taper question is answered by this same file. Baseline diff vs C00 is noisy (different piece/position) — a clean redo on the real C00 follows as CAP-C31-SEAM-TAPER. | Opening a piece puts it in the icon strip above the canvas; clicking the icon attaches it to the cursor and the next canvas click drops it (drop preview is anchored at the piece centre). Dropping triggered a zoom-to-fit of every piece in the work area, which moves everything on screen — re-map the canvas after any Open. Hovering a piece updates the piece-name field in the status bar: use it to identify pieces before acting. |
| CAP-C31-SEAM-TAPER | On the **real** re-opened CAP-C00-BASE (identified by hover). Intended Manual-Even 1.0 first, then Uneven TL=0.5, TR=0.25; the Even pass did not take (the tool re-opens with the previous seam type selected — my 1.0 + OK in Uneven mode just ended the tool), so the file holds Uneven only. Save As, export ASTM + ZIP. | `RESULT: PASS` after fixing the validator (see notes) — 3164 bytes, category CAP-C00-BASE, piece_records=2, 13 runs / 3 structural vs C00. Segments (0,1969), (0,984), none, none; cut lines L04 (1969,1)→(1,78669) and L08 (1,78669)→(120291,79669). Block-0 perimeter now starts at x=1969: record 0 is normalised so the **cut** outline's minimum sits at the origin, so a seam shifts the sew-line coordinates. | Define Seam remembers the last Seam Type radio — always click the one you want. The ASTM DXF writes layer-14 (sew) segments **only for seamed edges**; `verify_capture.dxf_check` now merges layers 14+1 and aligns by the dominant vertex translation instead of bounding-box minima (the old check reported a 7.87 in residual here). |
| CAP-C02-SAVEAS-NOEDIT (ad hoc) | File→Open CAP-C00-BASE again (PDS warns it is already in the icon menu — OK), drag from icon, drop on empty canvas, **no edit**, File→Save As→CAP-C02-SAVEAS-NOEDIT, export ASTM + ZIP. | `RESULT: PASS` — 1542 bytes, **piece_records=1**, 0 structural runs vs C00 (name, 4 heap bytes, timestamps only). So open + drop + Save-As under a new name does **not** create the second piece record; C50/C30/C31 got theirs from the edit. The 21-char name fits in the same 1542 bytes: the 0x15 header name and the trailer name are fixed-width slots (the longer name overwrote residue/zero padding), unlike the length-prefixed strings in the piece record. | "Open" of a piece already in the icon menu shows a warning dialog and does not open a second copy; drag the existing icon instead. Dropping a second instance re-zoomed the view only slightly this time. |
| CAP-C20-RULE-DISTINCT | RuleTable.exe: Numeric, smallest 2, base 8, step 2, **Next Size Breaks 4,6,8,10,12,14,16,18** (8 rows); Rules tab rule 1 with X 0.10…0.80 / Y −0.05…−0.40 per row; Save As CAP-RULES-A. PDS on the C02 piece: Grade→Assign Rule Table→piece→CAP-RULES-A→OK→piece→OK (status size 12→8). Grade→Rule Number: Track mode, clicked a corner → Point Id **4** populated (the tool marked the bottom-right corner although I clicked top-left), D1 cleared + `1`, Apply, OK. Save As, export ASTM + ZIP (Include Grade Rule Table stays greyed out). | `RESULT: PASS` — graded_points=1 (point 4 → object id 1), rul_n_rules=1, 2908 bytes, piece_records=2, 31 structural runs vs C02. **Object record decoded**: `i32 id=1, i32 0, then 8 × (dx, dy)` = (393,−196) (787,−393) (1181,−590) (1574,−787) (1968,−984) (2362,−1181) (2755,−1377) (3149,−1574) — the per-row increments in 1e-4 in, truncated, smallest break first; then a zero record id 10001. The size-list u16 at +6 = 7 = rows−1, so the record stride is 8+8·rows (48 bytes only because round 1 had 5 rows). Tail section now also carries the piece's previous name `CAP-C02-SAVEAS-NOEDIT` at 0x353. | RuleTable: rows are defined by the Next Size Breaks list starting from the smallest size — breaks 10…18 give "2-10, 10-12, …" (5 rows, no 8-10 row); breaks 4…18 give all eight 2-4 … 16-18 rows. `Type` with `clear:true` in a RuleTable grid cell opens the **Go To** dialog and swallows the keystrokes — double-click the cell and type without clear. In the Tracking Information dialog clicking a point switched the toggle to "Stop" by itself; typing into D1 then worked with the toggle reading "Track". |
| CAP-C21-RULE-TWO | RuleTable: added rule 2 (X 1.00 / Y 0.00 in all 8 rows) to CAP-RULES-A, Save. PDS: first attempt on the work-area C20 piece went wrong — typing into the **Point Id** field renumbers the point ("Point number is not unique" popup) — so that copy was deleted from the work area and the saved CAP-C20-RULE-DISTINCT re-opened from storage. Working loop: Rule Number → click a corner (Point Id fills, label flips to "Stop") → press the toggle (label "Track", typing enabled; the displayed point may change — read it) → when it showed point 1, clear D1, type 2, Apply, OK. Save As, export ASTM + ZIP. | `RESULT: PASS` — graded_points=2 (pt1→rule 2, pt4→rule 1), rul_n_rules=2, 3094 bytes, 23 runs / 12 structural vs C20: n_object_records 2→3 at 0xce, a new 72-byte record `id=2, 0, 8×(3937,0)` inserted after rule 1's record, point 1's record gains `f1=0` + `rule_ref=2`, and two 50-byte tail structures (`00 0500 040a 0200… 0f0a ffff…`) appear — one per graded point. Referenced rules are embedded with their values under their own number; the 10001 zero record stays. | Tracking Information dialog, corrected: fields are disabled until a point is tracked; **typing works only while the toggle reads "Track"**; a canvas click picks an unpredictable corner and flips the label to "Stop"; pressing the toggle re-enables typing but may switch the displayed point — always read Point Id before typing; never edit the Point Id box (it renames the point). D1 keeps the previous point's value — clear it (End, Shift+Home, Delete) before typing. |
| CAP-C22-RULE-NONE | Re-opened the saved CAP-C02-SAVEAS-NOEDIT (icon strip → drop at an empty spot), Grade→Assign Rule Table→CAP-RULES-A (double OK), **no Rule Number step**, Save As, export ASTM + ZIP. First attempt lost a click to a transient tool-classifier refusal, so Save As never got a piece and PDS refused the export ("modified … not saved — save and export?"); cancelled that prompt (saving would have overwritten the stored C02) and redid Save As. | `RESULT: PASS` — graded_points=0, all four points `f1=1` with no rule_ref, object records = `10001` only, rule_table CAP-RULES-A, n_break_rows 8, 2722 bytes, piece_records=2. vs C20: rule 1's 72-byte record and the two 50-byte tail blocks are gone (34 structural runs, most of them record-1 position bytes). vs C02: only 2 structural runs. So `f1 = 0x0001` = no grade rule; assigning a table embeds nothing but does create the second (pre-edit) piece record. | If a click is refused by the harness classifier mid-chain, PDS's state diverges silently — check the status-bar name before exporting. The "save and export?" prompt's OK/Cancel are at (1049,605)/(1159,605). |
| CAP-C10-PENT | Create→Rectangle (fresh, ~29.95×20.11 cm), named at creation. Modify→Split (line-actions)→clicked the top edge midpoint → new vertex created. Modify→Point Actions→Single Point→selected that vertex→moved it upward ~35cm to peak the roof, OK. Save As, export ASTM + ZIP. Hit two snags recapped below. | `RESULT: PASS` after fixing the validator (see notes) — 2840 bytes, perimeter_points=5, dxf residual 0.000000, piece_records=2. **L-label order for a 5-sided piece**: grain `L04`, then perimeter segments labelled `L00, L01, L06, L02, L03` — a jump to L06 between the 2nd and 3rd segment, confirming the capture plan's "L04, L08…L11 jumps for reasons unexplained" is real and reproduces on this piece too. Record 1 (the stale pre-edit duplicate) has the **original 4-sided rectangle** (`L04,L00,L01,L02,L03`, no jump) — direct confirmation that record 1 = pre-edit geometry, this time for a genuine shape edit rather than a seam/drill addition. | (1) A PDS session hiccup (a transient tool-classifier refusal mid-chain, then a "Gerber Technology License Manager — session killed due to network malfunction" popup) desynced Claude's model of canvas state from the actual UI for several turns; **PDS itself kept running licensed and undamaged throughout** — `Get-Process` + a fresh screenshot confirmed the app was fine, the only casualty was my tracking of piece positions after unexpected view scrolls/zooms. (2) When a Save As looks wrong (unexpected default filename, piece not where expected), don't trust in-session narration — re-verify by hovering the piece and reading the status-bar name field, or reopen the saved copy from storage (File→Open) and inspect its actual geometry before doing anything destructive. (3) `verify_capture.py`'s `facts()` used to hard-slice `segments[:4]`, a round-1 assumption baked in from all-quadrilateral captures — it silently truncated a 5-sided piece's segment list. Fixed to slice by the piece's actual perimeter-point count. |
| CAP-C11-HEX | First attempt: split the top edge at (800,700), tried again at (799,700)/(800,700) believing the click hadn't registered (no visible marker), then split the right edge similarly at (500,673)/(499,672), then bottom at (800,850) — moved two points and exported. Decoder showed **perimeter_points=8**, not 6: every "it didn't seem to work" click had actually placed a real, separate split point at a nearly-identical position, invisible at that zoom. Deleted the piece (Modify→Delete Piece from Work Area) and rebuilt from scratch: Create→Rectangle (fresh), Split top edge **once**, screenshotted to confirm exactly one new marker before doing anything else, moved that point up (Single Point→click→click destination→OK), then Split the bottom edge once, confirmed one marker, moved it down. Save As (overwrite prompt → Yes → Replace, not Append), export ASTM then ZIP (overwrite → Yes). | `RESULT: PASS` on the rebuild — perimeter_points=6, segment_points 2×6, dxf residual 0.000000 with a perfect (0,0) alignment shift (unmoved piece). The first, contaminated 8-point export was silently overwritten in storage and on disk — no orphaned bad capture remains. | **The Split tool's "Select point to split line" message never changes and gives no positive confirmation — a click that "did nothing" almost always did something.** After every Split click, screenshot immediately and count the new corner markers before clicking again; never retry a split click on the assumption it missed. `Save As`/`Export` over an existing piece/file prompts Overwrite/Replace — routine here, not a sign of desync, but always read which piece name is in the prompt before confirming. |
| CAP-C40-NOTCH-TYPES | Fresh rectangle (Create→Rectangle). Create tab→Notch group→**Standard** (not a dropdown on Standard itself — the panel that opens, "Add Standard Notch", has its own **Notch: Type:** numeric dropdown listing 1–30, plus Depth and Notch Type radio Single/Double). Placed 4 notches on the piece's left edge at varying distances from a corner, picking Type 1, 2, then attempting 3 (the list had re-scrolled and I actually got 4), then 8. Save As, export ASTM + ZIP. Two PDS windows/instances made locating the right canvas non-trivial this session — see notes. | `RESULT: PASS` after fixing the decoder (see notes) — notches=4, **notch_types=2;4;5;1**, perimeter_points=8, dxf residual 0.000000. **The PDS "Notch Type" number (1–30) is stored directly in the high byte of the perimeter point's `f1`** — `f1 = (type << 8) \| 0x01`, low byte 1 identifies "notch on an unnumbered point". Confirmed against the DXF: layers POINT/3 (4 entities) and POINT/4 (1 entity) plus 14 TEXT labels distinguish notch shapes on the drawing side too. The decoded types (2,4,5,1) don't match the UI selections I intended 1:1 (I meant 1,2,3,8) — the dropdown's list-repositioning-on-reselect makes it easy to pick the wrong row; the binary is authoritative regardless of what the UI intended. One of the four notches landed on the top edge rather than the left edge (canvas scale drift between clicks, same class of issue as earlier sessions) — harmless for this capture's purpose (type decoding) but means it doesn't answer the "same edge, ordered" part of the plan; that's still open for a redo if edge-assignment order specifically needs confirming beyond what C42 below shows. | Two AccuMark processes were running with overlapping "Work Area 1" tab names; `App{mode:switch}` matched the wrong one twice. **Reliable fix**: click the taskbar's "PDS MFC Application - N running windows" group button to get thumbnail previews of every open PDS window/tab, and pick by the piece count in the title (e.g. "5 piece(s)") or by the thumbnail's visible content, not by title-text matching alone. The Notch panel's Type dropdown re-scrolls to put the *current* selection at the top of the visible list every time it's reopened — always screenshot after opening it and read the row positions fresh rather than reusing a remembered y-offset. |
| CAP-C41-NOTCH-WIDTH | First attempt (fresh rectangle, Type 1 notch placed, then tried to change **Depth:** before placing a second) was lost: PDS crashed mid-session (unrelated to this capture — a long idle gap let the host machine sleep) and the AutoSave-recovered work areas (6 of them, one with 19 pieces matching the main session) did not contain this piece at all, confirming it had never survived an autosave checkpoint. Redone from scratch in the 19-piece work area: Create→Rectangle, named at creation, Create→Notch→**Standard** (Type still remembered as 1 from C40, Depth showing 0.40), placed two notches on the bottom edge at different x-positions, Save, File→Export ASTM then ZIP into the pre-made folder. | `RESULT: PASS` — 2881 bytes, perimeter_points=6, notches=2, **notch_types=1;1**, dxf residual 0.000000. **Depth is not stored anywhere in the piece file**: the two notch point records (`id=-1 f1=257 f2=0`, 14 bytes each) are byte-identical apart from `x` — confirmed by first establishing that the panel's **Depth:** field has no accessible `Edit` control in the UI tree (only `Type:` is a `Combo Box`), so it cannot be set independently of Type; two same-Type notches are therefore the only test the UI permits, and they show no daylight between the records beyond position. | (1) **After PDS Export or Save on a piece near the top of the scrolled canvas, the view can re-scroll so the piece sits partially above the visible area (y<356) with no way to scroll it back into full view via mouse wheel or the pane's own scrollbars** — the fix used here was to click on whatever fraction of the piece *is* visible (even a single edge) rather than fight the scroll; all edges of a simple rectangle remain individually clickable even when the piece's bounding box is partly clipped. A `Modify→Move Piece` attempt to relocate it first did not register (reference-point click failed silently) and was abandoned in favor of the partial-visibility workaround. (2) PDS's Export dialog still appends `.DXF` even when the typed filename already ends in `.dxf` (`NAME.dxf`→`NAME.dxf.DXF`), and separately exports the piece's `.RUL` under a similarly doubled name (`NAME.RUL.DXF` here, content is genuinely `.RUL` format) — type the bare name with no extension, or rename after export as done here. (3) Long idle gaps between tool calls let the host machine's screen sleep, which this session correlated with the PDS crash — treat any resumed session after a gap as suspect and re-verify piece counts/names via the taskbar and window titles before trusting prior in-context state. |
| CAP-C42-NOTCH-ALLEDGES | Fresh rectangle, larger than usual (~29×29 cm on screen) so all four edges would be individually clickable at once. `View→Zoom→Selected` (after picking the piece from the `...` piece-name lookup's "Current Pieces" list) framed the whole piece perfectly in one shot — the reliable fix for the recurring "piece scrolled partly off-screen after Save/Export" problem from C41, far better than fighting the scrollbars edge-by-edge. Create→Notch→Standard, placed one Type-1 notch on each of the four edges (top, right, bottom, left) in the same tool session without re-opening the panel, then exited via Point/Drill→Cancel, Save, File→Export ASTM (bare filename this time, no doubled `.DXF`) then ZIP. | `RESULT: PASS` — 3062 bytes, perimeter_points=8 (4 corners + 4 notches), notches=4, **notch_types=1;1;1;1**, `--expect segment_points=3;3;3;3` passes, dxf residual 0.000000 with DXF's own `L1 polyline points=[3, 3, 3, 3]` independently agreeing. **Fixed a real `verify_capture.py` bug this capture exposed**: `facts()` sliced the concatenated-across-all-piece-blocks `s['segments']` list by perimeter-*point* count (8), which happened to equal edge count on every earlier notch-free or single-notch-per-piece capture but here pulled in 4 extra, stale, pre-notch L-line records (`n_points=2`) from the piece's second (stale pre-edit) block, reporting `segment_points=3;3;3;3;2;2;2;2` instead of the correct 4-entry list. Fixed to slice by the count of numbered (`id != -1`) perimeter points, i.e. true edge count. | `View→Zoom→Selected` (ribbon Zoom group, "Selected" toggle button) is the fast, reliable way to frame a piece that's off-screen or partly clipped after a Save/Export-triggered re-scroll — far better than paging scrollbars or `Modify→Move Piece`. The `...` button next to the piece-name field opens a "Current Pieces" list (filterable) that at least confirms a piece's exact name even when clicking on canvas seems to select something else; selecting an entry there doesn't itself navigate the canvas, but does update the piece-name field so Zoom→Selected has the right target armed. Multiple notches can be placed in one Standard-Notch tool session (Type/Depth stay set) — no need to reopen the panel between edges. |
| CAP-C61-MIRROR | Fresh rectangle. `Modify→Piece Actions→Fold Keep` at first rejected the piece's own perimeter edge as the fold line ("Invalid Fold line.") — a **fold line must be an internal line**, not a perimeter edge. Drew one with Create→Line→**2-Point** straight down the middle of the rectangle (top edge midpoint to bottom edge midpoint), then Fold Keep→select that internal line→checked **Mirror Piece**→entered seam allowance 0.00→clicked the left half to keep. The tool immediately executed and renamed the piece to an auto-generated **"P5"** — Fold Keep does its own piece-level operation and does not preserve the original piece name; had to `File→Save As→CAP-C61-MIRROR` (Save Pieces) to get the intended name back into storage, then export ASTM + ZIP from there. | **RESOLVED (follow-up session)** — the original crash diagnosis above ("3 extra zero u16 fields, 11 vs 8") was wrong; the real cause is exactly **one** extra field. `len_annot` had always been read as a u32, whose high 16 bits were coincidentally 0 in every prior capture, masking that they're actually a separate field (`unk_u16_annot`) — 0 everywhere except this Fold Keep piece, where it's **2**. Fixed by splitting `len_annot` into a true u16 plus the new named field (byte-for-byte identical layout, so all 15 prior captures still decode unchanged — full `selftest.py` re-run confirms zero regressions). That alone fixed the crash but not the geometry: metadata's `n_perimeter` reads 4 for what DXF confirms is a true 4-corner rectangle, but only **3** explicit point records exist before the file runs straight into the internal-line list — no closing record, no 4th point at all. `parse_point_run` now stops defensively the moment it detects an internal-line header where a point was expected, instead of trusting `n_perimeter` blindly (safe: a real point can never collide with that check — see accumark_pds.py). With both fixes, `CAP-C61-MIRROR` decodes cleanly: perimeter_points=3, dxf residual **0.000000**. Leading theory for the missing 4th corner, tied to how Fold Keep actually works: the corner that lies on the fold line is implied by reflection rather than stored explicitly — unconfirmed pending a second Fold Keep sample. See FORMAT_SPEC.md §2/§4 and `selftest.py`'s R2 entry. | **"Fold Keep" requires selecting an internal line, not a perimeter edge**, as the fold line — clicking a piece's own boundary edge always returns "Invalid Fold line." regardless of which edge or where along it you click. The workflow is: draw a real internal 2-Point line first, *then* Fold Keep→Select Line→click that internal line→check Mirror Piece→enter a seam allowance value (0.00 accepted)→click the half of the piece to keep. The operation completes immediately on that last click (no OK button needed) and **silently renames the piece to an auto-generated "Pn" name** — always follow it with `File→Save As→Save Pieces` to restore the intended name in storage before exporting, since the in-canvas piece-name field does not reliably support direct renaming (typing into it did not stick in this session). |
| CAP-C60-CUTOUT | Fresh rectangle. Create→Line group→**Circles** dropdown (Curved Intersection / Center / 2 Point-Center / 3 Point / Tangent 1 Line / Tangent 2 Lines / Oval Orient / Oval Focus) → **Center**. The panel that opens has a **"Create New Piece"** checkbox, **checked by default** — unchecked it first so the circle becomes an internal feature of the selected piece rather than spawning a second independent piece. Clicked a center point well inside the rectangle, then a second point for radius; the circle rendered fully inside the piece boundary. Save, export ASTM then ZIP. | `RESULT: PASS` — 3654 bytes, perimeter_points=4 (rectangle unchanged), **cutout_points=25**, dxf residual 0.000000 (checked against the rectangle only — the DXF check only reads layers 1/14). **New internal-list kind found and decoded**: header `0xFFFF/0x0049` (vs grain's `0x0000/0x0047`, drill's `0xFFFF/0x0044`), 25 plain 14-byte points, explicitly closed (last point repeats the first), terminated by `u32=6` instead of grain/drill's `u32=3`. The DXF exports the same circle twice at different tessellations (layer 8: 25 pts matching the binary exactly; layer 85: 49 pts, a finer display curve) — confirms 25 is what PDS itself computed for this circle's polygon approximation, not an artifact of DXF export. `accumark_pds.py` and `verify_capture.py` extended (`INTERNAL_TAGS`, `cutouts_in`, `cutout_points`) — see FORMAT_SPEC.md §5.2. | **The Circles tool defaults to creating a brand-new independent piece** — the "Create New Piece" checkbox in its panel is checked out of the box every time it's opened, and must be unchecked before clicking the center point if the goal is an internal feature of the currently-open piece. This is the likely mechanism behind `CAP-C63-MODEL` (two genuine pieces in one export): leaving that checkbox checked while a piece is selected is a plausible, easy way to end up with two real pieces sharing one save/export rather than the already-documented stale-duplicate-record case. |
| CAP-C62-DART | Fresh rectangle. **Advanced tab → Darts group → Add** ("Add Dart"): clicked the bottom edge for the opening point ("Select opening point"), clicked a point above it inside the piece for the apex ("Select new dart apex"), then typed **2.00** for "Enter dart width." The dart rendered immediately as a triangular spike cut into the bottom edge. Save, export ASTM then ZIP. | `RESULT: PASS` after fixing two real decoder bugs this capture exposed (see notes) — 3144 bytes, piece_records=2, **perimeter_points=7** (4 corners + 2 dart-leg points + 1 apex, cut directly into the perimeter rather than stored as an internal line), dxf residual 0.000000. **`f2` is a trailer byte count, not a 0/1 flag**: every prior sample only ever had `f2 ∈ {0,1}`, so a dart-leg point's `f2 == 2` (two trailer bytes, not one) silently misaligned every point read afterward into garbage-huge coordinates. The apex point carries a new attr-byte value `0x12` (vs the known `0x09` turn / `0x0A` curve); the two dart legs' first trailer byte reuses `0x09` but their second byte differs (`10` vs `11` seen here) — pairing/meaning unconfirmed. See FORMAT_SPEC.md §4. | **Fixed two independent decoder bugs found via this capture, both defensive/bounds issues rather than misunderstood format fields**: (1) `parse_object_records` had no bounds check, so a false-positive metadata match during `summarize()`'s brute-force offset scan (landing inside the garbage produced by the `f2` misalignment above) handed it `n_object_records=14385, n_rows=13874` — building a ~400M-element list and hanging for minutes, since `i32()`/`u16()` never raise on a short slice (Python silently zero-fills past a bytes object's end) so nothing else caught the bad candidate. Now raises if the computed record array would run past EOF. (2) `decode_piece_block`'s internal-line reader looped `for _ in range(cnt)` with no bound either, for the same reason — now stops at EOF like `parse_point_run` already did. `summarize()`'s own block-scan loop also gained `decode()`'s existing 8-block safety cap plus a check that `block_end` actually advances, as defense in depth against any future false-positive candidate that "succeeds" without consuming bytes. All three fixes are purely defensive: a genuine file can never trigger them, confirmed by a full `selftest.py` pass with zero regressions after each one. |
| CAP-C63-MODEL | Two fresh rectangles, each created with **"Add Piece to Model"** checked in the Rectangle panel. The first piece's OK triggered an **"Enter New Model Name"** save dialog (separate from the piece name) — typed `CAP-C63-MODEL` there, which opened a **new work-area tab** titled `CAP-C63-MODEL - 1 piece(s)` (a genuine AccuMark Model, distinct from a plain Work Area). Second rectangle named `CAP-C63-MODEL-B`, same checkbox, joined the same tab (now "2 piece(s)") with no further prompt. File→Save (Save Models radio, select the model, OK), then File→Export→**Export Models** (not the usual Export Pieces) → ZIP into the pre-made folder. | **Answered the capture's question directly, no decoder bug involved**: a Model-level export is a completely different, much smaller container — the single `.tmp` in the ZIP is **621 bytes** (an ordinary rectangle piece is 1500+), starts with the usual magic and the *model's* name, but `accumark_pds.decode()` finds **zero** valid piece blocks — none of this format's per-piece metadata is present. It's a lightweight manifest/reference file, not an embedding of both pieces' geometry; each piece's actual geometry stays in its own separately-stored record. Tried switching to **Export Pieces** with both pieces present to see if that path embeds two full piece blocks in one file instead — that dialog only accepts a single piece selection per export, so it wasn't pursued further since the Model path already gives a clean, complete answer. See FORMAT_SPEC.md §8. | **"Add Piece to Model" checked in a piece-creation panel prompts for a separate Model name on the first piece only** (via "Enter New Model Name", a file-browser-style dialog rooted at `DATA90`) and switches the work area to a Model-titled tab; every subsequent piece created with the same checkbox joins that tab silently. **Export has an Export Models / Export Pieces radio** (seen for both Save and Export) — Export Models lists model names and, per its own on-dialog hint, bundling ALL models when exporting to ZIP pulls in every piece in the work area, not just the selected model's pieces (not verified further since only one model existed here). A Windows "Let's finish setting up / Device Setup" nag dialog popped up mid-flow, unrelated to PDS — dismissed without side effects. |
| CAP-C70-PASTED | File→Open→`CAP-C00-BASE` (Accumark Piece, fresh from storage into a new Work Area tab), dragged onto canvas. Create tab→Piece group→**Copy** with **Category: Copy Original** selected (the other option is "Piece Name"), clicked the piece to copy, clicked a second time to place the pasted copy (overlapping the original), typed `CAP-C70-PASTED` at the resulting piece-name prompt. Moved the copy off the original for clarity, then File→Save As→`CAP-C70-PASTED` (Save Pieces, select the copy — its default filename in that dialog was `CAP-C00-BASE`, the *category*, not the piece name I'd just given it, until overtyped), export ASTM then ZIP. No edit at any point after the paste. | `RESULT: PASS` — 1542 bytes, **category=CAP-C00-BASE** (confirms the category-is-the-original's-name finding directly rather than inferring it from six accidental instances), but **piece_records=1**, not the 2 this capture's plan entry anticipated. This isn't a contradiction: it sharpens the existing trigger rule from `CAP-C02-SAVEAS-NOEDIT` — the stale second record needs an edit *after* the piece is placed and named, and pasting a piece is not itself an edit. dxf residual 0.000000. See FORMAT_SPEC.md §8. | The Save As dialog's default filename for a just-pasted piece is its **category** (the original piece's name), not the name typed at the paste-placement prompt — read/overtype the File name field rather than trusting the default when saving a pasted piece under its intended name. Copy Piece's own placement click doesn't show a distinct "confirm position" step beyond the name prompt — click once on the source piece, then once more wherever the copy should land. |
| CAP-C12-TWOINTLINES | Fresh rectangle (its own grain line present by default). Create tab→Line group→**2-Point**, drew one plain open line fully inside the piece (not touching the perimeter or the grain line). Save, export ASTM then ZIP. | `RESULT: PASS` — 2846 bytes, perimeter_points=4 (unchanged), **the new line decodes as a second `cutout`-tagged internal list** (`internal_kinds=['grain','cutout']`, 2 points, `cutout_points=2`), header `0xFFFF/0x0049` — the *same* tag CAP-C60-CUTOUT's closed circle used, not a distinct fourth kind. Its terminator is `0x00000003` (open), matching grain/drill and confirming with a second independent sample that **the terminator (3 vs 6), not the tag byte, distinguishes an open line from a closed loop** — previously inferred from one sample only. Labels came out sequential (`L04` grain, `L05` the new line), no jump — see notes. dxf residual 0.000000. See FORMAT_SPEC.md §5.2. | **The canvas view can silently re-scroll right after switching ribbon tabs or clicking a tool button** (not just after Save/Export as documented for C41/C42) — two placement clicks landed on empty canvas below the piece before this was caught, because the piece had already scrolled up out of the intended click coordinates between selecting it and activating the 2-Point tool. Fix used: `View→Zoom→Selected` immediately before drawing, then screenshot once more right after activating the line tool to confirm the panel actually shows "Select first point for line" (an idle/empty User Input panel means the click on the ribbon button didn't register as tool activation, distinct from the tool being active but the canvas having moved) before clicking to place points. **On the jump question**: this capture's labels stayed sequential, so the `L04, L08…L11` jump seen on `CAP-C10-PENT` is not simply "more than one internal object" — it's more likely tied to editing perimeter points (a Split-line edit) after the piece's first save, which this capture never did. |
| CAP-C13-LONGNAME | Fresh rectangle, named with the 30-character `CAP-C13-LONGNAME-1234567890ABC` at the creation prompt (the field accepted it in full, no truncation observed in the UI). Save, export ASTM then ZIP. | `RESULT: PASS` — 1560 bytes vs `CAP-C00-BASE`'s 1542 (name is 18 characters longer, 30 vs 12) — **the difference is exactly 18 bytes**, nothing else changed. `_find_field_block` locates the metadata block at the identical offset `0x8a` in both files, confirming the **header** name slot (§1, 0x15) absorbed the extra 18 characters without shifting anything downstream (extends the fixed-width-slot finding from `CAP-C02-SAVEAS-NOEDIT`'s 21 characters to 30), while the **piece-record's** own `len(name)`-prefixed name string (§2) is what actually grew, by the same 18 bytes. `category`/`exported_name` both read back the full 30-character name intact. dxf residual 0.000000. See FORMAT_SPEC.md §1. | No new GUI behavior — the piece-name field in the Rectangle creation panel took the full 30-character string on the first try, no length limit hit. |
| CAP-C14-ANNOT | Create tab→Piece group→**Collar** (not Rectangle) with Collar Width 10, CB to Shoulder 5, Shoulder to CF 5, all other fields default, named `CAP-C14-ANNOT`. Save, export ASTM then ZIP. | `RESULT: PASS` — 1576 bytes, **annotation = "collar"** (lowercase, vs `RECTANGLE`'s uppercase) — confirms `annotation` is a literal per-creation-tool string, not a fixed enum or freeform field, and that reading it as a length-prefixed string (rather than a hardcoded `"RECTANGLE"` assumption) is correct. perimeter_points=5 (collar's natural 5-corner shape), dxf residual 0.000000. See FORMAT_SPEC.md §2. | **The Collar (and by extension Facing/Sleeve/Skirt/Fusible/Binding/etc.) creation panel's OK/Cancel/Apply buttons render below the visible screen area when the app is maximized on this display** — the panel has more fields (Dimensions, Notch, Fabric, Flips, Value Input) than Rectangle's, pushing the buttons past the taskbar boundary where clicks are swallowed by the Windows taskbar/Action Center instead of reaching the app. **Fix: drag the floating "User Input" panel by its title bar away from its docked position** (it un-docks into an independently movable/resizable window) **and reposition it near the top of the screen** so OK/Cancel render in a clickable area — un-maximizing the main window alone did not free enough space. This is a reusable fix for any future panel whose buttons are off-screen. |
| CAP-C36-SEAM-CORNERS | Six identical ~11.8×7.9cm rectangles, each named `CAP-C36-<STYLE>` for `SQUARED/SLANT/MITERED/EXTENSION/MIRRORED/TURNBACK`. `Advanced→Seam→Define` (Manual-Even, whole piece, 1.00cm), then `Advanced→Seam→Corners` dropdown (17 options total: Remove, Regular, Slant, Mitered, Double Miter, Tab, Extension, Mirrored, Turnback, Frame, Envelope, Perpendicular/Bisect/Slanted Step, Squared, Match Seam, Match Squared, Manual — only the 6 the plan named were exercised) applied to one isolated corner (Selection: Point). Exported all 6 via AccuMark Explorer's Export Zip (one piece at a time — multi-select via ctrl+click did not stick in this app's file list). | `RESULT: PASS` on all 6, decoded via `accumark_pds.decode_zip(...)['blocks'][0]['line_geometry']`. **Slant**: the corner becomes one diagonal segment directly joining each edge's own offset-line foot — no miter point at all. **Squared**: one extra right-angle step of exactly one seam-width (3937 units = 1cm) cuts the corner flat. **Turnback**: the seam line overshoots along each edge to the piece's *original* (pre-offset) coordinate before turning back to meet the seam line — a small rectangular tab, the turned-and-stitched finish. **Mitered, Extension, and Mirrored are byte-for-byte identical to each other** in every geometric field (confirmed by diffing the raw 3296-byte payloads directly): the only 12 differing bytes across all three sit at the same 12 offsets in each file and step by a constant +6 between MITERED→EXTENSION→MIRRORED — small record-id counters reflecting each file's own edit history, not corner-style data. Not a decode gap: a plain 90° corner with no configured extension length or fold axis has nothing for Extension/Mirrored to visibly change, so both degenerate to the same plain miter Mitered produces. See CAPTURE_PLAN.md's CAP-C36 entry and FORMAT_SPEC.md §10.1. | Multi-selecting several files at once in AccuMark Explorer's list via `ctrl+click` did not accumulate a selection in this session (each click replaced the previous one) — exported one piece at a time instead, which worked reliably. **A real near-miss worth remembering**: mid-session, `mcp__Windows-MCP__Snapshot` started reporting "No active window found" and unrelated windows (AccuScan, a second AccuMark Explorer) appeared; switching to "Pattern Design" by app name landed on a *different concurrent user's* live PDS window (`2303-B1-OUMO-1-SP24-TEST`) rather than this session's own — with two `sil2000.exe` (Pattern Design) processes running at once, switching by app name is ambiguous. Recovered by checking `Get-Process -Name sil2000` directly and waiting for the other session's instance to close rather than guessing which window was which. **Also found**: AccuMark Explorer's "Save As"/"Export Zip" dialog remembers its last-used destination folder across saves, but that folder can silently be a *different, stale clone* of the repo (`D:\Claude\Decoder\ClaudeScienceDecoder` here, not the actual working repo `D:\Claude\Decoder\extracted\pds_decoder_handoff`) if it was ever navigated there in an earlier session — always check the Export Summary dialog's own "To:" field against the intended path before clicking OK, not just the filename. |
| CAP-C32-SEAM-REMOVE | File→Open→`CAP-C30-SEAM-UNEVEN` (**Open Separate Work Areas** checked, so this session's other tab stayed untouched), dragged the loaded piece from the icon tray onto the canvas. `Advanced→Seam→Define` (Manual-Even), clicked the whole piece to select it, left the Value Input at its own default `0.00` and confirmed with a real `Return` keystroke (not the `Type` tool — the bottom-bar mode indicator flipping from `Sew` to `Cut` and the seam offset outline disappearing from the canvas were the live confirmation the removal took). Save As→`CAP-C32-SEAM-REMOVE`, exported ASTM+ZIP into the pre-made top-level `CAP-C32-SEAM-REMOVE/` folder (this plan's C30/C31/C32 fixtures live at the repo's top level, unlike the newer `captures/` convention `CAP-C36-SEAM-CORNERS` uses — first exported into `captures/CAP-C32-SEAM-REMOVE/` by mistake, caught by checking the existing empty placeholder folder, moved to the correct location). | `RESULT: PASS`, directly answers the capture's own question. Compared against `CAP-C00-BASE` via `accumark_marker.list_zip`/`accumark_pds.summarize_zip`: **all four `L00`-`L03` segment-attribute records clear to `seam_flag=0`, byte-for-byte matching a piece that was never seamed** — the logical removal is clean. But `n_blocks` stays **2**, not 1 — the stale second `piece_records` block inherited from `CAP-C30-SEAM-UNEVEN`'s own original seam-definition edit persists untouched; removing a seam is itself just another edit on an already-two-record file, not an undo of the earlier one. The only other byte difference from `CAP-C00-BASE` is one expected byte from a longer inherited category name (`TASK1-CUTQTY3`, 13 vs 12 characters) — not new residue. See FORMAT_SPEC.md §8. | Opening a file with **Open Separate Work Areas** checked places it in a small icon tray at the top-left of the new tab rather than directly on the canvas — **double-click the tray icon** to drop it onto the work area (a drag from the tray did not register). For an already-seamed piece, re-running Define Seam and accepting the pre-filled `0.00` (rather than needing to type a value) is sufficient to remove the seam — watch the bottom-bar `Sew`/`Cut` indicator and the seam outline disappearing as the live confirmation, since there's no separate "Remove Seam" command in the ribbon (the `Corners` dropdown's own "Remove" option is for corner *style*, not the seam as a whole). |
| CAP-C34-SEAM-CURVED | Two fresh rectangles. On each: `Modify→Curve`, clicked the whole top edge (both thumbtacks default to the edge's own endpoints), left-clicked again to accept the full-edge range, then placed a new point above the edge's midpoint to bow it into a smooth 3-point curve. `Advanced→Seam→Define` (Manual-Even): on `CAP-C34-SEAM-CURVED-POS`, clicked just the curved edge and typed `1.00`; on `CAP-C34-SEAM-CURVED-NEG`, clicked the piece near the curve intending to select only that edge but it registered as a whole-piece selection instead (all four edges got the value, not just the curve — a real, useful bonus rather than a mistake to redo, since it also exercises negative seam on straight edges in the same sample), typed `-1.00` via real keystrokes (the `-` key needed the literal `Shortcut` key name `"-"`, not a named key like `minus`, which errored). Saved both, exported via AccuMark Explorer into `captures/CAP-C34-SEAM-CURVED/` (a new folder, this plan's first use of the newer `captures/`-style location for a plain seam capture rather than a top-level pre-made folder, since none existed for this item). | `RESULT: PASS` on both, decoded via `accumark_pds.decode_zip(...)['blocks'][0]` and cross-checked with `check_line_table()`/`classify_line_table()`. Both already validate cleanly against the *existing* exact seam model with **no code changes**: `line_table_consistent=yes`, every point classifies as `seam_model_exact` or `stored_geometry` (one `axis_or_diagonal_fallback` on POS). Confirms genuine curve-normal offset math, not a naive per-axis shift — POS's curve peak moves from `(119342,244980)` to `(119342,249021)`, a 4041-unit shift against the nominal 3937 (1cm), because the offset is normal to the curve's own tangent rather than purely vertical; NEG's three plain straight edges each inset by exactly 3937 units, as the existing model already predicts. Added a dedicated `selftest.py` check (the folder holds two zips, so the generic single-fixture R2 list doesn't fit). See CAPTURE_PLAN.md's CAP-C34 entry. | The `Modify→Curve` tool's flow is: click a straight edge (both endpoints show as thumbtacks spanning the full edge already), click again to confirm the range applies to the whole edge, then click wherever the new curve point should sit — no drag needed. Clicking *near* a curved edge for `Define Seam` can register as a whole-piece pick instead of an edge pick depending on exactly where the click lands, unlike a plain straight edge's midpoint where this was reliable in earlier captures — worth clicking closer to the visible line itself, or explicitly checking `check_line_table`'s output afterward, when precision matters. |
| CAP-C35-SEAM-TAPER-TRUE | Fresh rectangle. `Advanced→Seam→Define` (Manual-Uneven), clicked the left edge's start corner and typed `0.50`, then clicked its end corner and typed `1.50` (real keystrokes) — both nonzero and unequal, unlike `CAP-C30`/`CAP-C31`'s taper which always ramped from a zero `begin`. Every other edge left unseamed. Saved, exported via AccuMark Explorer into a new `captures/CAP-C35-SEAM-TAPER-TRUE/` folder. | `RESULT: PASS`. Decoded: `begin=5906` (1.50cm) and `end=1969` (0.50cm) match the two entered values exactly — with only one seamed edge on the piece, there's no adjacent-segment interaction to complicate the reading, so this is the cleanest possible confirmation of FORMAT_SPEC.md §6.1's existing begin/end rule (begin = value at the edge's own start corner, end = value at its own end corner) rather than a new finding. The cutline's two points sit exactly `begin`/`end` units inward of the edge's own start/end corners, zero residual; `check_line_table()` returns `True` and both points classify as `seam_model_exact`. No code changes needed. See FORMAT_SPEC.md §6.1. | None — this capture worked exactly like the established Manual-Uneven corner-click flow from `CAP-C30`/`CAP-C32`. |
| CAP-C37-SEAM-SWAP | Opened `CAP-C35-SEAM-TAPER-TRUE` on canvas (already had this session), `Advanced→Seam→Swap`, clicked the piece — a warning dialog named the piece and noted "Performing Swap Sew/Cut without Adjust Seam Grading may cause graded piece shapes to change" (harmless here, ungraded piece), then a second dialog warned the piece "has uneven seam... Not all functions will be allowed while the piece is in the swapped state." The swap visibly exchanged which outline was solid (the perimeter) vs dashed (the seam offset) on canvas. Saved As `CAP-C37-SEAM-SWAP`, exported into a new `captures/CAP-C37-SEAM-SWAP/` folder. Also tried to build a fold-axis piece for the plan's second half (a seam on a fold line): Create→Line→**Mirror** turned out to duplicate geometry across a chosen line rather than mark an edge as the piece's own fold axis, and Advanced→Darts→**Fold** ("Fold Dart") needs an existing dart and folds it closed — neither is the right tool, abandoned rather than forced (matches the earlier `CAP-C33` non-completion). | `RESULT: PASS` on the Swap half. Compared decoded structure before/after: the stored **perimeter** takes on the *old offset seam-line's* coordinates exactly, the new **seam record** takes on the *old perimeter's* coordinates exactly, `begin`/`end` both negate (`5906/1969`→`-5906/-1969` — the allowance is now measured in the opposite relative direction), and the swapped edge's line label changes (`L00`→`L05`, a freshly allocated slot, not reused) — consistent with the existing "labels are per-creation-order" finding. `check_line_table()` returns `True` on both the pre- and post-swap files with zero code changes. See FORMAT_SPEC.md §6.1. | The Swap Sew/Cut confirmation dialogs are genuinely informative, not just noise — the first names the exact piece being swapped (useful to confirm you selected the right one before committing), the second is a live, correct warning that persists as real UI state afterward (some ribbon commands do become unavailable on a swapped piece, matching what it says). |
| CAP-C37-FOLD-SEAM | Retried the plan's second half after finding the right tool: `CAPTURE_LOG.md`'s own `CAP-C61-MIRROR` entry already documented `Modify→Piece Actions→Fold Keep` as the fold-axis mechanism, missed on the first attempt (which tried `Create→Line→Mirror` and `Advanced→Darts→Fold`, neither correct). Fresh rectangle, `Create→Line→2-Point` down the middle (an internal line is required — Fold Keep rejects a perimeter edge as the fold line, per the existing `CAP-C61-MIRROR` note), then `Fold Keep`→selected that internal line→**Mirror Piece** already checked by default→typed `1.00` at its own "Enter the seam allowance value for the split line" prompt→confirmed→clicked the left half to keep (operation completes immediately on that click, silently renaming the piece to an auto-generated `P6`, exactly as `CAP-C61-MIRROR` documented). `File→Save As→CAP-C37-FOLD-SEAM` to restore the name, exported into a new `captures/CAP-C37-FOLD-SEAM/` folder. | `RESULT: PASS`. **The fold axis decodes as `internal_kinds=['grain','mirror']`** — the first controlled, non-production confirmation that Fold Keep produces the same `0x4d`/mirror tag already decoded from the `OUCF` production capture. **The seam allowance entered for the split line is not stored anywhere in the file**: every perimeter segment reads `seam_flag=0`, and a raw byte search for `3937` (1.00cm) as both `i32` and `u16` across the entire 1409-byte payload finds zero matches — a clean negative result, not an absence of evidence. See CAPTURE_PLAN.md's CAP-C37 entry. | Confirms `CAP-C61-MIRROR`'s own documented workflow exactly: an internal line is mandatory (a perimeter edge is rejected), Mirror Piece is checked by default, the operation needs no OK button and fires on the final "select piece to keep" click, and the piece is always renamed to an auto-generated `Pn` requiring a follow-up Save As. Worth remembering for next time: **when a capture plan item names a specific PDS tool or behavior, grep `CAPTURE_LOG.md` for it before hunting through ribbon tabs** — the exact recipe may already be documented from an earlier, differently-motivated capture. |
| CAP-C37-FOLD-NOMIRROR | A `CAP-C37-FOLD-SEAM` byproduct: `FORMAT_SPEC.md`'s own field table had an open `[?]` since round 2 — piece-record offset `+0x04` reads `2` on `CAP-C61-MIRROR` (the only prior Fold Keep sample) and `0` everywhere else, "meaning unconfirmed pending a second Fold Keep sample." Having just built one (`CAP-C37-FOLD-SEAM`, which also reads `2`), the natural control was one more Fold Keep piece identical in every way except the **Mirror Piece** checkbox. Fresh rectangle, `Create→Line→2-Point` down the middle, `Fold Keep`→select that line→**unchecked** Mirror Piece (the only difference from the `CAP-C37-FOLD-SEAM` recipe) → left the seam-allowance prompt at its own default `0.00` → kept the left half. Saved As `CAP-C37-FOLD-NOMIRROR` (again auto-renamed to a `Pn` first), exported into a new `captures/CAP-C37-FOLD-NOMIRROR/` folder. | `RESULT: PASS` — **resolves the `[?]`.** This piece reads `mirror_flag=0` (not `2`) and, tellingly, has **no `mirror`-tagged internal line at all** (`internal_kinds=['grain']` only — the fold line is *not* preserved as a tagged internal line when the piece isn't mirrored, it's simply consumed by the split). Across three independent samples now (`CAP-C61-MIRROR`=2, `CAP-C37-FOLD-SEAM`=2, this one=0), the field cleanly tracks whether Fold Keep's Mirror Piece checkbox was used — confirmed by direct manipulation of that one checkbox, not correlation. Renamed the decoder's own field from `unk_u16_annot` to `mirror_flag` in `accumark_pds.py`, added it to `verify_capture.py`'s `facts()`, and to `selftest.py`'s regression coverage on all three samples. *Why* the flag reads `2` specifically rather than a plain boolean `1` remains open — a smaller, cosmetic loose end, not a blocker. See FORMAT_SPEC.md §2. | None — worked first try, same recipe as `CAP-C37-FOLD-SEAM` with one checkbox flipped. |
| CAP-C82-FOLD-OBLIQUE | A wide, clearly non-square rectangle (~8:1 aspect ratio). First tried the exact corner-to-corner diagonal as the fold line (matching what `CAP-C61-MIRROR`'s own recorded geometry implied) — Fold Keep rejected it outright with "Invalid Fold line," a real, previously-undocumented validation rule (an internal line whose endpoints coincide with two of the piece's own corners is not accepted). Redrew the internal line from a point on the top edge to a point near-but-not-at the bottom-right corner instead — clearly oblique, non-45°, touching two edges without hitting either corner. `Fold Keep` (Mirror Piece checked) accepted this line, selected the top region to keep. Also found: `Match 2 Pts` fold-line selection mode does **not** use the two picked points as the line's own endpoints — it computes something else entirely (rendered as a near-vertical line through the piece's middle, i.e. more consistent with a perpendicular-bisector-style construction) — abandoned that mode once `Select Line` (clicking a few pixels off either endpoint, away from the corner markers) was confirmed to work reliably. Saved As, exported into a new `captures/CAP-C82-FOLD-OBLIQUE/` folder. **Named C82, not C80** — `CAP-C80-BOOKMARK` is a different, still-open, already-reserved plan item (testing PDS's own Bookmark/Restore Original feature for §11's snapshot hypothesis), caught only after the first write-up already used that name by mistake; renamed the capture and fixed the docs before finalizing. | **Settles the question, with one honest surprise along the way.** Checked pairwise reflection between stored points instead of just counting stored-vs-missing corners (that count came back a clean `n_perimeter=5`/5-stored, no ambiguity to inspect directly, which is what first looked inconclusive). Points `id3` `(89464,12782)` and `id7` `(89464,1)` reflect onto each other across a shared axis (through `(89464,6392)`/`(41537,6392)`) to a residual of exactly **1 native unit (0.0001 in)** — essentially exact; `id6`, sitting exactly on that axis, reflects to itself with zero residual. Since this axis is genuinely oblique (not axis-aligned, not 45°, not a diagonal), "complete the bounding box" isn't a coherent alternative for this pairing — only true geometric reflection produces it. This is the non-axis-aligned sample `CAP-C61-MIRROR` couldn't be: **Mirror Piece performs genuine reflection**, closing FORMAT_SPEC.md §10.2 for good. **The surprise, resolved the following day (offline, no new capture needed)**: that reflection axis is *not* the piece's own independently-decoded `mirrors_in` value, which reports a completely different line — the perimeter's own `id5`-`id8` edge, not a plausible reflection axis at all. Checked field-by-field, not just by coordinate: the `mirror`-tagged internal line's two points are a byte-for-byte copy of perimeter points `id5`/`id8` — same `x`/`y`, same `attr=9`, same placeholder `rule_ref=10001` — the two corners Fold Keep created where the drawn fold line crossed the piece's original edges, not independently-stored axis geometry. **`mirrors_in` was never answering "what's the axis" — it answers "which two corners bound the fold."** The genuine reflection axis has no perimeter-point identity of its own; it lives only in the line table's own `kind=2` echo. Not a discrepancy between two measurements of the same thing, but two different (both correct) pieces of information sharing one `mirror` tag. See FORMAT_SPEC.md §4/§10.2/§10.3. | Two reusable technique notes: (1) an internal line with both endpoints exactly on existing piece corners is rejected by Fold Keep as "Invalid Fold line" — use an oblique line touching two edges at non-corner points instead. (2) `Match 2 Pts` picks two points to define the fold line by some rule other than "use them as the line's own endpoints" — stick with `Select Line` (clicking the actual drawn line, a few pixels off either end to avoid the corner markers) when the exact fold-line geometry matters. (3) Before naming a new capture, grep the plan docs for that exact name first — `CAP-C8x` names are pre-reserved for specific planned experiments, not free-form. |
| CAP-C80-BOOKMARK | Four related samples off one fresh rectangle, finally using the real Bookmark tool (`Edit` tab → `Bookmark` group: `Define`, `Restore Original`, `Restore Defined` — three separate ribbon buttons, not a dropdown). (1) `CAP-C80-BOOKMARK`: drew the rectangle, saved, clicked `Define`→selected the piece — sets a bookmark on the current state, no visible dialog. (2) Without ever calling Define, built a byte-for-byte identical rectangle named `CAP-C80-BOOKMARK-CONTROL` as the "never bookmarked" control. (3) On the bookmarked piece, `Modify→Move`'d the top edge up ~1cm and Saved As `CAP-C80-BOOKMARK-MODIFIED` (bookmark still defined, one real edit made). (4) `Restore Defined`→selected the modified piece: it does **not** revert in place — it places a brand-new copy of the piece at its *bookmarked* (pre-edit) geometry next to the current one and prompts for a name; named it `CAP-C80-BOOKMARK-RESTORED` and saved. All four exported via AccuMark Explorer into `captures/CAP-C80-BOOKMARK/`. | **Answers §11's snapshot hypothesis with a clean negative, plus one genuine positive found along the way.** `CAP-C80-BOOKMARK-CONTROL` (1158B) vs `CAP-C80-BOOKMARK` (1150B) differ by exactly 8 bytes — precisely the piece-name length difference (`-CONTROL` is 8 characters), same `piece_records=1`, same `region_c_consistent=yes`, identical geometry: **`Define` alone, before any edit, leaves no additional file trace whatsoever** — a bookmarked-but-unedited piece is byte-structurally indistinguishable from a never-bookmarked one (mod name length). This rules out the naive "Region C's snapshot1/snapshot2 pair *is* the bookmark, written the moment you call Define" reading. **But a genuine, bookmark-specific effect shows up once an edit follows**: `CAP-C80-BOOKMARK-MODIFIED` has **`piece_records=3`**, not the usual 2 the already-documented stale-second-record mechanism (§8) produces for a plain post-save edit — block 0 is the live (edited) geometry, and blocks 1 *and* 2 are both full, identical copies of the pre-edit geometry. That's a real, additional stale copy beyond the generic mechanism, and its timing (only appears once a bookmark was defined) ties it to Bookmark specifically rather than to editing alone. **A second, distinct, newly-found anomaly**: `CAP-C80-BOOKMARK-RESTORED` (the fresh piece `Restore Defined` creates) matches the bookmark's geometry exactly and passes `line_table_consistent`, but fails `region_c_consistent` — its Region C `snapshot2` decodes as clearly-garbage huge/negative coordinates (e.g. `1301348353`, `-626655229`), the same signature already catalogued for the three known desynced-snapshot outliers (`CAP-C30-SEAM-UNEVEN`/`CAP-C31-SEAM-TAPER`/`TASK2-SEAM1CM`) — **root-caused the same day, offline, by diffing `CAP-C80-BOOKMARK-RESTORED` byte-for-byte against the unedited `CAP-C80-BOOKMARK` (same geometry, every other byte identical)**: `RESTORED` has two new non-zero 4-byte fields (`289104` at offset 676, `132567` at offset 680) sitting in what is plain zero-padding on every other sample, right before the real `marker1`/`marker2` pair — which is itself untouched, still `10000`/`10000` at the *same* offsets (684/696) as `BOOKMARK`. So this is a genuine new PDS-written trace of the `Restore Defined` operation itself, not corrupted surrounding data — but `parse_region_c`'s marker-scan (grabs the first non-zero word, no way to recognize these two fields as extra) reads them as `marker1`/`marker2` by mistake, and that single misread cascades into a garbage `snapshot2`. Deliberately **not fixed this pass**: the two fields' own meaning is unconfirmed (as 0.0001in units they're 28.9104in/13.2567in — near but not exactly on this piece's own 21.6-36.2in × 7.8-18.7in bounding box, so "restore placement anchor" is a guess, not a finding), and patching the scan off one sample risks a fragile heuristic — a corpus sweep this same session found `marker1 != marker2` is already normal on every seamed/seam-swap fixture (small values like `1`/`2`, unrelated to this), so "markers must match" isn't a safe general rule either. Needs a second `Restore Defined` sample with different geometry before a real fix can be verified rather than guessed from n=1. See FORMAT_SPEC.md §11. | `Define`/`Restore Original`/`Restore Defined` are three distinct ribbon buttons under `Edit→Bookmark`, not a single tool with modes. `Restore Defined` (and, presumably, `Restore Original`) does not mutate the current piece in place — it **creates a new piece object** at the bookmarked geometry and prompts for a name, leaving the current (edited) piece untouched alongside it; both then need an explicit Save to persist. AccuMark Explorer's file list has a real, reproducible **stuck-selection bug**: after a list item briefly enters in-place rename mode (triggered by two clicks landing close together in time on the same row), every subsequent click anywhere near that row's former screen position keeps landing on that same item regardless of which row is actually clicked, even after Escape/F5/re-selecting a different row first — the fix that worked was navigating to the intended row with repeated `Up`/`Down` arrow key presses from a row that DID click correctly, rather than continuing to click blindly. |
| CAP-C80-BOOKMARK2 | A second, independent Restore Defined sample, built specifically to verify (rather than guess) the fix for `CAP-C80-BOOKMARK-RESTORED`'s `region_c_consistent=no` anomaly. Fresh rectangle with different dimensions and canvas position than the original `CAP-C80-BOOKMARK`; `Define`→bookmarked; `Modify→Move` on an edge (moved the right edge, not the intended left one — harmless, still a real single-edge edit) then Saved As `CAP-C80-BOOKMARK2-MODIFIED`; `Restore Defined`→placed the new piece at a *different* screen location than the first `RESTORED` sample (to the right instead of below) specifically to test whether the anomaly's mystery fields track canvas placement, named `CAP-C80-BOOKMARK2-RESTORED`. All three exported into the same `captures/CAP-C80-BOOKMARK/` folder. | **Confirms the fix generalizes, and pins down what the two mystery fields actually are.** `CAP-C80-BOOKMARK2-RESTORED` reproduces the exact same structural anomaly at the exact same relative byte offset (8 bytes before the real `marker1`/`marker2` pair) as the first sample, with different values (`360811`, `72394` vs `289104`, `132567`) — ruling out a one-off fluke. Checked what the two values actually represent: **they are this block's own bounding-box center** (`cx`, `cy`, rounded to the nearest native unit) — confirmed on both samples to within 0.5 units (float rounding): `(289104, 132567)` vs bbox center `(289104.0, 132566.5)` on the first sample, `(360811, 72394)` vs `(360810.5, 72394.0)` on this one. Not canvas placement (both samples were placed at different screen positions, yet the values track each block's own *piece-space* bbox, not screen coordinates) and not a session/record counter (values differ per-sample, tracking geometry). Fixed `accumark_pds.parse_region_c()`: it now computes the same bbox center from its own already-parsed `snapshot1`, checks whether the candidate `marker1`/`marker2` pair matches it (within 1 unit), and if so treats it as a new field (`restore_anchor`, `None` when absent) and continues scanning for the true marker pair — safe against misfiring on a genuine marker pair since those are always small (`10000`/`10000` or `1`/`2`), never geometry-scale. Both Restore-Defined samples now decode with `region_c_consistent=True`; `selftest.py`'s corpus-wide `record_state` sweep no longer needs to exclude `CAP-C80-BOOKMARK-RESTORED` by name. See FORMAT_SPEC.md §11. | None beyond what `CAP-C80-BOOKMARK` already documented — same Bookmark tool recipe, same AccuMark Explorer stuck-selection bug encountered again and worked around the same way (arrow-key navigation from a correctly-clicked row). |
| CAP-C33-ANNOTATION | Fresh rectangle. `Create→Annotation`→clicked inside the piece→typed `TEST` in the "Add Annotation" text box→OK→clicked a location to place it (default 0° Font Rotation, "Plot Preference" character size). Saved, exported as a first checkpoint. Then, on the same piece, repeated `Create→Annotation` with text `ROT` and Font Rotation explicitly set to `45` (typing into the field required select-all-then-Delete first — a plain `ctrl+a` re-type appended instead of replacing) — placed it lower on the piece, saved over the same file, re-exported into `captures/CAP-C33-INTERNAL-TYPES/`. | **Answers the capture's real question with a clean negative, and surfaces a genuinely new, undecoded structure.** Neither annotation — not even the 45°-rotated one, which the manual's own wording ("determines width, height, and rotation for annotation characters") suggested should need the 3-point (`B`) line form — produced any internal-line-list entry (`internal_kinds` stayed `['grain']` both times). Modern `Create→Annotation` does not use the `A`/`B` internal-line mechanism at all; annotation text is stored in its own dedicated record instead, found and partially characterized by diffing the two exports byte-for-byte: a `u32` constant `52`, a `u16` string length, the raw text bytes with **no padding** to any fixed boundary (confirmed on the 3-character "ROT"), then a `u32` constant `28`, an `i32` X and `i32` Y position — further fields (including wherever the 45° rotation is actually stored) not yet located. Each annotation's record appears to repeat immediately afterward, plausibly matching the dialog's own "Annotation Library" panel. **A genuine new decoder gap found as a side effect**: this new record type sits somewhere `parse_point_snapshot`/Region C's own parsing doesn't expect, desyncing `snapshot1` from its very first point on ANY piece with an Annotation object (`snapshot1`'s bogus first point literally decodes the ASCII "TEST" as coordinates) — the identical failure signature as the already-documented seamed-live-block bug, but a distinct trigger. `check_region_c()` already reports this correctly as `False`; `selftest.py`'s `record_state` check needed a one-line fix to skip blocks failing `check_region_c()` (it wasn't filtering on that before, unlike the other two Region-C-aware checks) rather than excluding this fixture by name. See FORMAT_SPEC.md §5.4/§11/§12. | The `Annotation` dialog's `Font Rotation` field does not respond to `ctrl+a` (select-all) before typing a replacement value the way a normal text box would — it appends instead of replacing, silently producing a garbage combined value (e.g. `0.0045` instead of `45`). Reliable fix: click the field, `End`, `shift+Home`, `Delete`, then type the new value. Also: `Create→Annotation` sometimes places the FIRST click as if it selected a location immediately (skipping the dialog) and sometimes opens the "Add Annotation" text dialog first depending on prior tool state — always screenshot after the first click to see which prompt is actually showing before typing. |
| CAP-C33-ANNOT-ROTA/-ROTB | A controlled follow-up to `CAP-C33-ANNOTATION`, built to isolate the rotation field left unlocated there. Fresh rectangle; `Create→Annotation`, typed `SAME`, left Font Rotation at its default `0.00`, clicked the same canvas location as later — saved as `CAP-C33-ANNOT-ROTA`, exported. Then, on the same still-open work area, `Create→Annotation` again with the identical text `SAME` at Font Rotation `45` clicked at the same canvas location — this *replaced* the existing "SAME" annotation in place rather than adding a second one alongside it (confirmed visually: only one "SAME" ever showed on canvas), saved under a new name `CAP-C33-ANNOT-ROTB` (not overwriting ROTA) so both the 0° and 45° states exist as separate files, exported into the same `captures/CAP-C33-INTERNAL-TYPES/` folder. | **Rotation fully decoded.** Byte-diffing the two exports (same length, same text, same clicked position) narrows the whole file down to 17 differing bytes; all but one 8-byte span are small heap-residue/name-echo noise already familiar from elsewhere in this project. The one substantive difference: an 8-byte field that reads all-zero in `ROTA` (0°) and reads `18 2d 44 54 fb 21 e9 3f` in `ROTB` (45°) — decoded as a little-endian IEEE-754 double, that's exactly `0.7853981633974483` = `radians(45)`, bit-for-bit. **Rotation is stored as a double, in radians, not degrees or a fixed-point integer.** Its exact byte offset relative to the annotation record's own other fields (found here by absolute-position diffing, not by walking the record structure) isn't pinned down yet, and the earlier "record repeats a second time, maybe an Annotation Library echo" idea from `CAP-C33-ANNOTATION` did not clearly reproduce at the expected position here — left open rather than forced. See FORMAT_SPEC.md §5.4. | Placing a second annotation with identical text at (approximately) the same position replaces the existing one rather than adding a duplicate — useful to know when trying to build a controlled pair differing in only one property: save each state under its own piece name *before* making the next change, since the live work area won't hold both simultaneously. |
| CAP-C33-ANNOT-ECHO (offline follow-up, no new capture) | Both open questions left by the ROTA/ROTB row above resolved by offline analysis of files already on disk — no new PDS session. Hex-dumped the full region between the ROTA/ROTB primary record and the already-known rotation offset (956); precisely field-parsed it with `struct` rather than eyeballing hex, then re-ran the same field layout against `CAP-C33-ANNOTATION` (the original, independently-captured TEST/ROT sample never used to derive this structure) as a generalization check. | **Both questions closed.** (1) Every Annotation object is written twice: a primary record at creation, plus a fixed-shape 36-byte "echo" record later in the file, found by scanning for its own `u32 1, u32 52` header rather than at a fixed offset. The echo's exact layout: `u32 1, u32 52, u32 text_length, i32 X, i32 Y, u32 0, f64 rotation_radians, i32 -1, u32 0` — the rotation field sits at a fixed **+20 bytes** from the echo's own start, not merely "found by diffing." Cross-checked on 5 independent instances (`TEST`, `ROT`×2, `SAME`×2) — `text_length` matches each annotation's own string length exactly every time, and rotation reads exactly 0° or `radians(45)` matching each one's actual Font Rotation. (2) The "repeats a second time" question: **it does, but the count matches the number of same-text annotation objects on the piece, not a fixed rule** — `CAP-C33-ANNOTATION`'s two `ROT`-labeled echoes (different X/Y, both 45°) exist because that capture's edit history left two separate `ROT` objects (most likely one stale, one live), not because of an "Annotation Library" double-write mechanism. Added `find_annotation_echoes()` to `accumark_pds.py` (a standalone scan, not yet wired into `decode()`'s sequential cursor) and a selftest check validating it against all 3 captures. See FORMAT_SPEC.md §5.4. | n/a — offline byte analysis only, no GUI session. |
| CAP-C33-LINELABEL-C(-EDITED) | Live attempt at the still-open `C`/`P`/`T` internal-line codes, using the `CAP-C33-ANNOT-ROTB` rectangle as a base. (1) `Create→Line→2-Point`, drew a diagonal internal line; its own panel offered a "Use Line Label" checkbox + dropdown listing every letter A-Z/digit — selected `C`, clicked OK, saved as `CAP-C33-LINELABEL-C`, exported. (2) On the same piece, `Edit→Line` opened a docked "Tracking Information" panel (not a modal dialog) with `Label:`/`Name:`/`Seam Amount:` fields matching the manual's own terms; clicking the drawn line populated `Label: I` (matching the byte-level tag exactly) and `Name: L08`. Edited `Label:` to `C`, tried both `Apply` and pressing Enter to commit, saved as `CAP-C33-LINELABEL-C-EDITED`, exported. | **Clean double negative, thoroughly checked at the byte level both times.** Decoding `CAP-C33-LINELABEL-C`: the drawn line's raw tag byte is `0x49` (`'I'`, generic internal — `INTERNAL_TAGS`'s existing catch-all), not `0x43`; its `internal_labels` name is the auto-numbered `L08`, with zero trace of the chosen `C` anywhere. So the 2-Point Line tool's "Use Line Label" dropdown is unrelated to internal-line typing entirely — some other naming/reference field, not investigated further. Decoding `CAP-C33-LINELABEL-C-EDITED`: byte-for-byte identical to the un-edited version at the tag offset (still `0x49`) and everywhere else in the internal-line header — the `Label:` edit in the Tracking Information panel never persisted at all; re-selecting the same line afterward showed it reverted to `I` on its own. That panel is a live tracking/HUD readout that happens to *display* the true type letter correctly (confirming `I`↔`0x49`, `G`↔`0x47` as already known), not an editable "Line Info" surface, despite matching the manual's field names. See FORMAT_SPEC.md §5.4. | The "Tracking Information" panel that `Edit→Line` opens is **docked, not modal** — closing it via its own `Cancel`/✕ also cancels whichever tool opened it (the `Line` selection tool, in this case), so to keep editing after dismissing an unrelated leftover instance of the same panel, re-activate the ribbon tool from scratch rather than expecting the panel alone to reappear. It also has no built-in drag handle visible without first grabbing its title bar - `Move{drag:true}` from the title bar works to reposition it out of the way of a small/zoomed-out piece before clicking on canvas geometry underneath it. |
| REGION-C-SEAM-REANCHOR (offline fix, no new capture) | Root-cause investigation of the seam-triggered half of Sec 12's Region-C runaway-snapshot bug, using the 12 seamed-live-block samples already on hand (`CAP-C30-SEAM-UNEVEN`, `CAP-C31-SEAM-TAPER`, `CAP-C34-SEAM-CURVED-NEG/POS`, all six `CAP-C36-SEAM-CORNERS` fixtures, `CAP-C37-SEAM-SWAP`, `CAP-C35-SEAM-TAPER-TRUE`) - no new PDS session. Hex-dumped the byte range between `record_state` and the garbage `snapshot2` read directly (rather than guessing offsets) on `CAP-C35-SEAM-TAPER-TRUE`, then scanned that same range on all 12 samples for the raw perimeter's own first point (`perim[0]`) reappearing verbatim, and tested whether reading `n_perimeter` points from there produced a clean match. | **10 of 12 fixed.** The gap between `record_state` and `snapshot2` on a seamed block isn't a small fixed shift (the single-sample guess from the first pass) - it's a variable-length (100-200+ byte) structure containing real seam-corner/allowance-shaped data (a point offset from a real corner by a plausible seam-allowance delta, `10000`/`10000` constants) that reading straight through desyncs into the already-documented `id=512,x=65536` garbage. Fixed `parse_region_c()`: try the immediate read first (every unseamed block untouched), and if it doesn't validate against `snapshot1`'s own coordinate set, search forward for `perim[0]` (not `snapshot1[0]` - confirmed on `CAP-C36-SLANT` that `snapshot1` can start at a different rotation of the same corners, which would anchor on a coincidental later match) and re-anchor there. 10 of the 12 samples now decode with `region_c` fully consistent; `CAP-C30-SEAM-UNEVEN`/`CAP-C31-SEAM-TAPER` (both already flagged as not fitting a plain per-corner offset model) still don't validate. Coverage rose accordingly on the fixed samples (e.g. `CAP-C35-SEAM-TAPER-TRUE` 96.23%→98.01%, `CAP-C37-SEAM-SWAP` 95.42%→97.17%). One new gap surfaced by the fix: `unclassified_gap`'s `v40` formula (§11) doesn't hold on any of the 10 newly-passing seamed blocks (each off by a different amount) - excluded from that selftest check pending its own investigation rather than force-fit or silently regressed. Added a dedicated selftest check naming all 12 samples by name and asserting the exact expected pass/fail split, not just an aggregate pass count. See FORMAT_SPEC.md §12. | n/a — offline byte analysis and decoder fix only, no GUI session. |
| REGION-C-UNEVEN-SEAM-STRUCTURE (offline investigation, no new capture) | Follow-up on the 2 seamed samples the fix above didn't resolve (`CAP-C30-SEAM-UNEVEN`, `CAP-C31-SEAM-TAPER`). The anchor-search fix finds their true perimeter-echo point fine, but the resulting snap2 still garbles from the second point onward - byte-searched for each corner's own real coordinates individually (not just point 0) to measure the actual stride between corners, rather than assuming the standard 15-byte point size. | **Root cause pinned down precisely, decode not yet completed.** Each corner's snap2 entry on these two samples is a **46-byte compound record**, not the plain 15-byte one: a normal 15-byte point (matching real perimeter exactly), immediately followed by a second, independently-valid 15-byte point record at the same array index but a different `x` (`y` unchanged) - plausibly a per-corner seam-value override, given "uneven"/"tapered" is exactly what these two samples are testing - then a fixed 16-byte tail, **byte-for-byte identical on both samples**: `02 00 00 00 02 00 00 00 02 00 00 00 00 02 00 00`. `parse_point`'s self-reported size (15) under-advances by exactly 31 bytes per corner, which is why the previous fix's anchor search finds corner 0 correctly but everything after it is garbage. Not fixed this pass - `parse_point_snapshot` isn't taught this 46-byte stride, and the middle point's real meaning (which exact seam value it holds) and the 16-byte tail's meaning are both still unconfirmed - but any future attempt now has an exact, cross-validated record boundary to work from instead of guessing offsets. See FORMAT_SPEC.md §12. | n/a — offline byte analysis only, no GUI session. |
| CAP-C33-ANNOT-CORNERS (offline follow-up, no new capture) | Follow-up on the Annotation primary record's own "long tail" (the four tagged points, still undecoded after the echo-record pass). Ruled out "annotation text's own bounding box" first by checking it against a fact already on hand: the tail is byte-identical between `CAP-C33-ANNOT-ROTA` (0°) and `-ROTB` (45°), which a rotated text quad could not be. Then compared the four points directly against `decode()`'s own `perimeter` field for the same piece. | **Resolved.** The four points are the host piece's own perimeter corners, verbatim - not a new structure at all, just a third echo of geometry already fully decoded elsewhere (alongside Region C's snapshot1/snapshot2). The record's own point tags read `2,3,4,1`, a one-off rotation of the perimeter's natural `1,2,3,4` order, the same harmless rotation Region C's own `snapshot1` already exhibits. Between the two corner listings sits the familiar `marker1`/`marker2`-style `10000`/`10000` pair Region C already uses for "unseamed" - re-embedded here rather than a new constant. So the whole tail is a compact, self-contained echo of the host piece's own Region-C-style snapshot, bundled inside the Annotation object. Not fully closed: what decides the short (`"TEST"`-style) vs. long (corner-echo) tail form is still unconfirmed, and the second corner listing's own trailing bytes don't fit a plain fourth point cleanly - both left open rather than forced. Added a selftest check that finds the primary record by its own header and verifies its four points equal `decode()`'s own perimeter, corner-for-corner. See FORMAT_SPEC.md §5.4. | n/a — offline byte analysis only, no GUI session. |
| REGION-C-REANCHOR-MULTICANDIDATE (offline refinement, no new capture) | Refinement of the seam-triggered Region-C fix: instead of stopping at the first byte-offset where the perimeter's own first point reappears, `_reanchor_point_list()` now tries every occurrence in the search window and keeps the first one where all `n_perimeter` points fully validate. Also attempted extending the same re-anchoring technique to `snapshot1` itself, to fix the separate Annotation-triggered desync (`CAP-C33-ANNOTATION`). | **The multi-candidate refinement fixed the last 2 seamed samples: all 12 of 12 now decode with `region_c` fully consistent** (up from 10/12) - `CAP-C30-SEAM-UNEVEN`/`CAP-C31-SEAM-TAPER`'s first coordinate match sits inside the 46-byte per-corner structure documented in the row above, producing one good point before desyncing; a later occurrence in the window is the true, fully-valid list. **The `snapshot1` extension was tried, then explicitly reverted**: it initially looked correct (`snapshot1` came back matching real perimeter on every annotation-bearing sample) but a second, independent signal disagreed - `record_state` started reading `0` (stale) on 5 single-block samples that cannot be stale. Root cause: the Annotation record's own tail (previous row) independently contains an exact echo of the same perimeter corners, so a coordinate-only search can't tell Region C's true `snapshot1` apart from a coincidental match inside that unrelated structure - both "pass" the same narrow test. Reverted rather than shipped on a check that turned out to be too weak an oracle once a second genuine copy of the perimeter existed elsewhere in the file. `CAP-C33-ANNOTATION` still correctly fails `check_region_c()`, not silently masked. See FORMAT_SPEC.md §12. | n/a — offline byte analysis and decoder fix only, no GUI session. |

## 2026-09-09 — marker session (no GUI; files supplied by the user)

Inputs: `2303-BD 137 marker.zip` (unlaid), `2303-BD 137 PLACED.zip` (laid, 97
placements), `2303 PATTERN-MODELS.zip` (whole style, July vintage), and the
July `2303-CP 150 CPL` set (4 corner-probe markers + drawn DXFs). Findings and
the resulting decoder/validator work are in `MARKER_DECODE_PLAN.md` (status
table at the top). Headline: marker placement format solved and verified
(slots, binding, W/L/U identity, DXF centres ≤0.0008 in); `accumark_pds`
gained four fixes exposed by 162 production pieces (point-table locator,
corner-notch rule group, header residue, fold unfolding) plus a 5000×
`summarize()` speed-up; `CAP-C61-MIRROR`'s "missing 4th corner" turned out to
be the locator bug (spec §4 corrected). `selftest.py` now also runs the six
markers in `markers/`.
